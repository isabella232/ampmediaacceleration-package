import XCTest
@testable import AmpMediaAcceleration

final class AmpMediaAccelerationTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AmpMediaAcceleration().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
